
<%- include ("header")-%>



    <div class="box" id="heading">
        <h1><%= listTitle %> </h1>
    </div>

    <div class="box">
     <!-- <% for (let i=0;i<newListItem.length;i++){ %>
        <div class="item">  
            <input type="checkbox" >
             Implementado para llamar a los componentes de la base de datos y que tengan el componente name
            es importante senalar el elemento al cual se agregara en la lista dependiendo del item
            <p> <%= newListItem[i].name %></p>
        </div>   
        <%   } %>-->
        <% newListItem.forEach(function(item){ %>
        <form action="/delete"  method="post">

            <div class="item">  
                <input type="checkbox" name="checkbox" value="<%= item._id%>" onChange="this.form.submit()" >
                <p> <%= item.name %></p>
            </div>
            <input type="hidden" name="listName" value="<%= listTitle%>">
        </form>
        <%})%>
        <!-- Seccion creada para anadir nuevos elementos-->
        <form action="/"  method="post" class="item">
        
            <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
            <br>
            <button type="submit" name="list" value="<%= listTitle %>">+</button>
        </form>
            
    </div>

<%- include ("footer") -%>